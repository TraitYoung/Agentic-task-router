[project]
name = "specforge"
version = "2.0.0"
description = "SpecForge: AI-powered software engineering spec generation and review system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]
